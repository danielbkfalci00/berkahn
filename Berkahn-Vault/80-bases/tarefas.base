filters:
  or:
    - file.hasTag("status/draft")
    - file.hasTag("status/wip")
    - file.hasTag("todo")

formulas:
  prio_num: 'if(prioridade == "alta", 1, if(prioridade == "media", 2, 3))'

properties:
  titulo:
    displayName: "Tarefa"
  prioridade:
    displayName: "Prioridade"
  due:
    displayName: "Prazo"
  projeto:
    displayName: "Projeto"

views:
  - type: table
    name: "Em aberto"
    order:
      - titulo
      - projeto
      - prioridade
      - due
      - file.mtime
    sort:
      - property: due
        direction: ASC

  - type: table
    name: "Por prioridade"
    groupBy:
      property: prioridade
      direction: ASC
    order:
      - titulo
      - projeto
      - due
    sort:
      - property: formula.prio_num
        direction: ASC

  - type: table
    name: "Por projeto"
    groupBy:
      property: projeto
      direction: ASC
    order:
      - titulo
      - prioridade
      - due
    sort:
      - property: formula.prio_num
        direction: ASC
